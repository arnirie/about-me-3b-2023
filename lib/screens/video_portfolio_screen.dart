import 'package:flutter/material.dart';

class VideoPortfolioScreen extends StatelessWidget {
  const VideoPortfolioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
